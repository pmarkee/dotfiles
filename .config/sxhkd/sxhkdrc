# TODO finish cleanup
# TODO look into preselection stuff

### OVERVIEW ###
# MOD key is alt
# shift is used as kind of a "modifier" key that somewhat alters the original function, like calling with different args or something
# Anything marked with a "#WHAT" comment is something I don't understand
#
# General categories:
# mod + _ -> programs and scripts (except terminal)
# alt + {_, shift} + _ -> anything very often used (sound control, navigating windows etc)
# alt + ctrl + {_,shift} + _ -> less often used but still possible (moving windows, preselect and all that)
# super + ctrl + _ -> system control (shutdown, reload, etc)

### PROGRAM SHORTCUTS ###
alt + Return
        $TERMINAL

super + d
	dmenu_run

super + b
        $BROWSER

super + m
        $MUSIC

super + r
        $DRAW

super + h
        # Stuff for copying hungarian characters via dmenu.
        hun_chars

### SOUND CONTROL ###
# Increase volume
alt + {_, shift + }i
        amixer sset 'Master' {5%+,20%+}
# Decrease volume
alt + {_, shift + }o
        amixer sset 'Master' {5%-,20%-}
# Toggle mute
alt + u
        amixer sset 'Master' 1+ toggle

### WINDOW CONTROL ###
# close and kill
alt + {_,shift + }q
	bspc node -{c,k}

## focus/swap ##
# focus or send to the given desktop
alt + {_,shift + }{1-9,0}
	bspc {desktop -f,node -d} '^{1-9,10}'

# focus or move the node in the given direction
alt + {_,shift + }{h,j,k,l}
	bspc node -{f,s} {west,south,north,east}

# focus the last node/desktop
alt + {_,shift} + Tab
	bspc {node,desktop} -f last

alt + shift + {Left,Right}
        bspc desktop -f {prev,next}.local

alt + shift + {Up,Down}
        bspc node -f {prev,next}.local

# swap the current node and the biggest node
# Do I need this?
alt + shift + g
	bspc node -s biggest

## move/resize ##
# expand a window by moving one of its side outward
alt + ctrl + {h,j,k,l}
	bspc node -z {left -50 0,bottom 0 50,top 0 -50,right 50 0}

# contract a window by moving one of its side inward
alt + ctrl + shift + {h,j,k,l}
	bspc node -z {right -50 0,top 0 50,bottom 0 -50,left 50 0}

# move a floating window
alt + {Left,Down,Up,Right}
	bspc node -v {-50 0,0 50,0 -50,50 0}

# focus the older or newer node in the focus history
alt + {_,shift} + v
	bspc wm -h off; \
	bspc node {older,newer} -f; \
	bspc wm -h on

## state/flags ##
# set the window state
alt + {t,p,g,f}
	bspc node -t {tiled,pseudo_tiled,floating,fullscreen}

# set the node flags
#WHAT
alt + ctrl + {m,x,y,z}
	bspc node -g {marked,locked,sticky,private}

### SYSTEM CONTROL ###
super + ctrl + b
        bspc wm -r && pkill -USR1 -x sxhkd

super + ctrl + s
        pkill -USR1 -x sxhkd

super + ctrl + r
        dmenu_prompt 'Shutdown?' 'sudo -A shutdown now'

super + ctrl + t
        dmenu_prompt 'Restart?' 'sudo -A reboot'

super + ctrl + z
        dmenu_prompt 'Lock screen?' 'xsecurelock'

super + ctrl + u
        dmenu_prompt 'Quit bspwm?' 'bspc quit'

