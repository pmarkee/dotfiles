#
# wm independent hotkeys
#

# MASSIVE TODO cleanup this whole file, what a fucking mess

### PROGRAM SHORTCUTS ###
# terminal emulator
alt + Return
        urxvt
# launcher
alt + d
	dmenu_run

alt + b
        firefox

alt + s
        subl3

### SHUTDOWN, LOGOUT, RELOAD ETC. CONTROL ###
# Reload sxhkd and bspwm (note, colon (:) is NOP in bash)
alt + {_, shift + }Escape
	{:,bspc wm -r} && pkill -USR1 -x sxhkd

alt + super + r
        dmenu_prompt 'Shutdown?' 'sudo -A shutdown now'

alt + super + t
        dmenu_prompt 'Restart?' 'sudo -A reboot'

alt + super + z
        dmenu_prompt 'Logout?' 'logout'

alt + super + u
        dmenu_prompt 'Quit bspwm?' 'bspc quit'

### SOUND CONTROL ###
# Increase volume
alt + {_, shift + }i
        amixer sset 'Master' {5%+,20%+}
# Decrease volume
alt + {_, shift + }o
        amixer sset 'Master' {5%-,20%-}
# Toggle mute
alt + u
        amixer sset 'Master' 1+ toggle
#
# bspwm hotkeys
#

# close and kill
alt + {_,shift + }q
	bspc node -{c,k}

# alternate between the tiled and monocle layout
alt + shift + m
	bspc desktop -l next

# send the newest marked node to the newest preselected node
alt + shift + y
	bspc node newest.marked.local -n newest.!automatic.local

# swap the current node and the biggest node
alt + shift + g
	bspc node -s biggest

#
# state/flags
#

# set the window state
alt + {t,p,f,g}
	bspc node -t {tiled,pseudo_tiled,floating,fullscreen}

# set the node flags
# TODO what the hell does this do?
alt + ctrl + {m,x,y,z}
	bspc node -g {marked,locked,sticky,private}

#
# focus/swap
#

# focus or move the node in the given direction
alt + {_,shift + }{h,j,k,l}
	bspc node -{f,s} {west,south,north,east}

# same but with arrow keys (does not work)
alt + {_,shift + }{left,down,up,right}
	bspc node -{f,s} {west,south,north,east}

# focus the node for the given path jump
alt + shift + {p,b,comma,period}
	bspc node -f @{parent,brother,first,second}

# focus the next/previous node in the current desktop
alt + {_,shift + }c
	bspc node -f {next,prev}.local

# focus the next/previous desktop in the current monitor
super + Tab
	bspc desktop -f {next,prev}.local

# focus the last node/desktop
alt + {_,shift} + Tab
	bspc {node,desktop} -f last

# focus the older or newer node in the focus history
alt + {n,m}
	bspc wm -h off; \
	bspc node {older,newer} -f; \
	bspc wm -h on

# focus or send to the given desktop
alt + {_,shift + }{1-9,0}
	bspc {desktop -f,node -d} '^{1-9,10}'

#
# preselect
#
# TODO is this section even relevant?
# preselect the direction
alt + ctrl + {h,j,k,l}
	bspc node -p {west,south,north,east}

# preselect the ratio
alt + ctrl + {1-9}
	bspc node -o 0.{1-9}

# cancel the preselection for the focused node
alt + ctrl + space
	bspc node -p cancel

# cancel the preselection for the focused desktop
alt + ctrl + shift + space
	bspc query -N -d | xargs -I id -n 1 bspc node id -p cancel

#
# move/resize
#
# TODO is this section even relevant?
# expand a window by moving one of its side outward
alt + super + shift + {h,j,k,l}
	bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}

# contract a window by moving one of its side inward
alt + ctrl + shift + {h,j,k,l}
	bspc node -z {right -20 0,top 0 20,bottom 0 -20,left 20 0}

# move a floating window
alt + {Left,Down,Up,Right}
	bspc node -v {-20 0,0 20,0 -20,20 0}
