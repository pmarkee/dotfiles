# MASSIVE TODO cleanup this whole file, what a fucking mess

### OVERVIEW ###
# MOD key is alt
# shift is used as kind of a "modifier" key that somewhat alters the original function, like calling with different args or something
# Anything marked with a "#WHAT" comment is something I don't understand
#
# General categories:
# alt + {_, shift} + _ -> anything very often used (launchers, sound control, navigating windows etc)
# alt + ctrl + {_,shift} + _ -> less often used but still possible
# super + {_, ctrl} + _ -> system control (shutdown, reload, etc)

### PROGRAM SHORTCUTS ###
# TODO launch these with env variables, does not work now for some reason
alt + Return
        urxvt

alt + d
	dmenu_run

alt + b
        firefox

alt + e
        urxvt -e nvim

alt + m
        spotify

### SOUND CONTROL ###
# Increase volume
alt + {_, shift + }i
        amixer sset 'Master' {5%+,20%+}
# Decrease volume
alt + {_, shift + }o
        amixer sset 'Master' {5%-,20%-}
# Toggle mute
alt + u
        amixer sset 'Master' 1+ toggle

### WINDOW CONTROL ###
# close and kill
alt + {_,shift + }q
	bspc node -{c,k}

# send the newest marked node to the newest preselected node
#WHAT
alt + shift + y
	bspc node newest.marked.local -n newest.!automatic.local

# swap the current node and the biggest node
# Do I need this?
alt + shift + g
	bspc node -s biggest

## state/flags ##
# set the window state
alt + {t,p,g,f}
	bspc node -t {tiled,pseudo_tiled,floating,fullscreen}

# set the node flags
#WHAT
alt + ctrl + {m,x,y,z}
	bspc node -g {marked,locked,sticky,private}

## focus/swap ##
# focus or send to the given desktop
alt + {_,shift + }{1-9,0}
	bspc {desktop -f,node -d} '^{1-9,10}'

# focus or move the node in the given direction
alt + {_,shift + }{h,j,k,l}
	bspc node -{f,s} {west,south,north,east}

# focus the last node/desktop
alt + {_,shift} + Tab
	bspc {node,desktop} -f last

# focus the older or newer node in the focus history
alt + {_,shift} + v
	bspc wm -h off; \
	bspc node {older,newer} -f; \
	bspc wm -h on

## move/resize ##
# expand a window by moving one of its side outward
alt + ctrl + {h,j,k,l}
	bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}

# contract a window by moving one of its side inward
alt + ctrl + shift + {h,j,k,l}
	bspc node -z {right -20 0,top 0 20,bottom 0 -20,left 20 0}

# move a floating window
alt + {Left,Down,Up,Right}
	bspc node -v {-20 0,0 20,0 -20,20 0}

### SYSTEM CONTROL ###
super + ctrl + b
        bspc wm -r && pkill -USR1 -x sxhkd

super + ctrl + s
        pkill -USR1 -x sxhkd

super + ctrl + r
        dmenu_prompt 'Shutdown?' 'sudo -A shutdown now'

super + ctrl + t
        dmenu_prompt 'Restart?' 'sudo -A reboot'

super + ctrl + u
        dmenu_prompt 'Quit bspwm?' 'bspc quit'

